name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: build (x86_64-linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            accept-flake-config = true

      # `nix flake check` builds every flake output for the current system
      # (packages.x86_64-linux.* and checks.x86_64-linux.*), so an explicit
      # "build all" step is redundant.
      - name: nix flake check
        run: nix flake check --print-build-logs
